You should have openCV installed in order to use this.

Compile the main.cpp using -> clang++ main.cpp -o app $(pkg-config --cflags --libs opencv4)

and run using -> ./app

ENTER THE ABSOLUTE ADDRESS OF THE SOURCE IMAGE AND OUTPUT TEXT!!
